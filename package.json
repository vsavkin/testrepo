{
  "name": "remix-next-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "reset": "git clean -fdX && npm install && npx lerna bootstrap"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
